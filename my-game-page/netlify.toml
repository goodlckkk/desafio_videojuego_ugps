[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"  # Asegura que las funciones SSR sean detectadas

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
